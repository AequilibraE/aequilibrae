"""Schema helpers shared across sources.

- ``modes``: access-based mode-rule engine (no link-type allow-lists).
- ``link_types``: deterministic, uncapped link-type allocator.
- ``attributes``: routing of free-form IR columns into ``other_attributes`` JSON.
"""
